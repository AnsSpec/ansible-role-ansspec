---
software_dir: "/opt/{{ software.name }}-{{ software.version }}"
software_bin_dir: "{{ software_dir }}/bin"
software_conf_dir: "{{ software_dir }}/conf"

software_log_dir: "/var/log/{{ software.name }}-{{ software.version }}"
software_tmp_dir: "/tmp/{{ software.name }}-{{ software.version }}"

username: "{{ software.name }}"
groupname: "{{ software.name }}"
servicename: "{{ software.name }}"